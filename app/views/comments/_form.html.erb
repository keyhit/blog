<%= bootstrap_form_with model: [@user, @post, @comment], remote: true do |form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.text_area :text %>
  <%= form.submit id: "new_comment"%>
  <%#= link_to 'Back', user_post_comments_path(params[:user_id], params[:post_id], params[:id]), type: 'button', class: 'btn btn-light' %>
<% end %>
